set cut_paste_input [stack 0]
push $cut_paste_input
Expression {
 expr0 "r > threshold ? 1 : 0"
 expr1 "g > threshold ? 1 : 0"
 expr2 "b > threshold ? 1 : 0"
 expr3 "a > threshold ? 1 : 0"
 name _THRESHOLD_
 selected true
 xpos -8200
 ypos -729
 addUserKnob {20 User}
 addUserKnob {7 threshold}
 threshold 0.99
}
